apiVersion: v1
kind: Service
metadata:
  name: {{ include "token-vendor-chart.fullname" . }}
  labels:
    {{- include "token-vendor-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "token-vendor-chart.selectorLabels" . | nindent 4 }}
